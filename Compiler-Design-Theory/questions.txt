186 What do you mean by ambiguity of a grammar? Explain with a suitable example? Consider the following grammar;
Show that the string ‘aa+a*’ can be generated by this grammar? Construct the parse tree? S -> S S + | S S * | a

187 What do you mean by ambiguity of a grammar? Explain with a suitable example? Consider the following grammar;
Show that the string ‘(( a ))’ can be generated by this grammar? Construct the parse tree? S -> ( S )| S+S | a

188 Consider the following grammar and draw the LR(0) automaton:
S -> L=R | L
L -> *R | id
R -> L

190 Construct the LR(1) automaton for the following grammar:
S′→ S
S → CC
C → cC | d

192 Show that the string ‘id + id * id’ has two distinct leftmost derivation for the following grammar. Draw both parse
trees by showing the construction at every step of the derivation.
E -> E + E | E * E | - E | ( E ) | id

193 Consider the following grammar;
S -> S S + | S S * | a
Show the leftmost derivation of string ‘aa+a*’?
Show the rightmost derivation of string ‘aa+a*’?
Is the grammar ambiguous or unambiguous?

194 Write down the regular expression for the following language;
Set of all strings whose lengths are divisible by 3, over ={a,b}
Set of all strings that begin and end with different symbols, over Σ={a,b}
Set of all strings that ends with ‘aab’ over Σ={a,b}
Set of all strings that begin with ‘bab’ over Σ={a,b}
Set of all even length strings over Σ={a,b}

195 Consider the following grammar;
S -> ( S )| S+S | a
Show the leftmost derivation of string ‘( a )+( a )’?
Show the rightmost derivation of string ‘( a )+( a )’?
Draw the parse tree?

196 Find the regular expressions of the following languages;
Set of all strings over
= {a, b} such that all the strings begin and end with different symbols.
Set of all strings over
= {a, b} such that all the strings begin and end with same symbol.

197 Remove the left recursion from the following grammar
A -> ε | xAy | AA
S -> Sa | aSSb | ε

198 Remove the left recursion from the following grammar
A -> Ab | aC
B -> BaBB | BA
C->bC | BA

199 Remove the left recursion from the following grammar
S -> Aa | Bb
A -> Aa | Abc | c | Sb
B -> bb

200 Remove the left recursion from the following grammar
S -> AA |0
A -> SS | 1

201 Remove the left recursion from the following grammar
S -> ABC
A -> Aa | Ad | b
B -> Bd | e
C -> Cc | g

202 Construct the predictive parsing table to check if the grammar is LL(1) or not?
S->aABb
A ->c | ε
B ->d | ε

203 Construct the predictive parsing table and parse the string "ab" by using the parsing table;
S ->AaAb | BbBa
A ->ε
B ->ε

204 Prove that the following grammar is LL(1). Parse the string ‘aa’
by using the parsing table;
S->SA | A
A->a

205 Construct the predictive parsing table to check if the grammar is LL(1) or not.
E -> TE'
E’
-> +TE' | ε
T -> FT'
T'
-> *FT' | ε
F -> ( E ) | id

206 Construct the predictive parsing table to check if the grammar is LL(1) or not.
S -> iCtS | iCtSeS | a
C -> b

210 Remove the left recursion from the following grammar. Construct the predictive parsing table of the grammar
obtained after removing the left recursion. Is the grammar LL(1)? justify
A->Ac | Aad | bd | ε

211 Consider the following itemset and Find out the itemsets obtained from the following GOTO functions:
I =
{
E'
-> E
E -> E + T | T
T -> T * F | F
F -> (E ) | id
}

GOTO (I, E), GOTO(I, T), GOTO(I, id), GOTO(I, ()):

213 Find the FIRST & FOLLOW the following grammar
A -> Ab | aC
B -> BaBB | BA
C->bC | BA

214 Find the FIRST & FOLLOW the following grammar
A -> Ba | Baa
B -> Ab | Abb

215 Find the FIRST & FOLLOW the following grammar
S -> Aa | Bb
A -> Aa | Abc | c | Sb
B -> bb

216 Find the FIRST & FOLLOW the following grammar 2 10 CO6 BL4 0
S -> AA |0
A -> SS | 1

217 Find the FIRST & FOLLOW the following grammar
S -> ABC
A -> Aa | Ad | b
B -> Bd | e
C -> Cc | g

218 Find the FIRST & FOLLOW the following grammar
A -> ϵ | xAy | AA
S -> Sa | aSSb | ϵ

219 Convert the following regular expression to a DFA;
x*yxx (y + x)*

222 What is the drawback of CLR parsing? How do we overcome it in LALR parsing? Write down the steps to convert a
CLR parsing table to LALR parsing table.

223 Construct an equivalent NFA of the following regular expression; (0+1)*(00+11)(0+1)*.
Convert that NFA to its equivalent DFA?

257a Optimize the following C-code:
count=0;
result= 0;
while(count++ < 20)
{
increment= 2*count;
result +=increment;
}
257bb Explain about Dead code elimination.

258a What is the purpose of code optimization? Explain in detail about Peephole Optimization with
exampleexamples
258b Define Loop optimization.

259 Explain the machine-dependent and independent techniques.

260a What is an activation record? Explain how it is related with run time storage organization.
260b Give the applications of DAG.

261a Discuss the various issues in code generation with examples.
261b Write short notes on basic blocks and flow graphs.

262a Define triples,indirect triples and quadruples.
262b Write three codes for x:=A[y, z] with detail explanation.

263A Write a note on the specification of a simple type checker.
263B List the various ways of calling the procedures. Explain in detail.

264a How would you convert the following into intermediate code.
    i. Assignments statements.
    ii. while statements.

264b Describe the method of generating syntax directed definition for control Statements.